name: "Docker Client"
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  short-name: "docker-client"
  keywords:
    - "docker"
    - "container"
    - "docker-api"
    - "containerization"
  categories:
    - "cloud"
    - "containerization"
  status: "stable"
# guide: "https://quarkiverse.github.io/quarkiverse-docs/quarkus-docker-client/"
  config:
    - "quarkus.docker."